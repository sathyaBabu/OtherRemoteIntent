<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="code.anint.edu.com.intentedu">

<permission android:name="com.edureka.permission.SATHYA_SHARE_POST"/>




    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <activity android:name=".otherActivityEdureka"

            >
            <intent-filter>
                <action android:name="code.anint.edu.com.intentedu.otherActivityEdureka" />
                <action android:name="Vivan" />

                <category android:name="android.intent.category.DEFAULT" />
            </intent-filter>

            <intent-filter>
                <action android:name="android.intent.action.VIEW" />

                <!-- this is my remark : DAta area -->
                <data android:scheme="mystreamer" />

                <category android:name="android.intent.category.DEFAULT" />
            </intent-filter>
        </activity>
    </application>

</manifest>